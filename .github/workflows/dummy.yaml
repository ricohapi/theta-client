name: Dummy for test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-12
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - uses: subosito/flutter-action@v2
    - name: Flutter Test
      run: |
        cd flutter
        flutter pub get
        flutter test
